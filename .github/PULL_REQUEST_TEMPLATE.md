<!-- One or two sentences: what does this change, and why? -->

## Checklist

- [ ] `make check` passes (lint, types, architecture contracts, tests)
- [ ] No new runtime dependencies — `dependencies = []` stays empty
- [ ] Wire shapes untouched or strictly additive (see CLAUDE.md, "frozen contracts")
- [ ] If this edits `skill/cv/`: the mirror under `plugin/skills/swissdevjobs/cv/` is updated too
